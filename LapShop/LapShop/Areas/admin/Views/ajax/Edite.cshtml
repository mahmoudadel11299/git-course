
@model TbItem
<div class="content-wrapper">
    <div class="page-header">
        <h3 class="page-title"> Form elements </h3>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="#">Forms</a></li>
                <li class="breadcrumb-item active" aria-current="page">Form elements</li>
            </ol>
        </nav>
    </div>
    <form class="forms-sample" method="post" enctype="multipart/form-data"
           role="form">
        <div class="row">
            <div class="col-md-6 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Default form</h4>
                        <p class="card-description"> Basic form layout </p>

                        <div class="form-group">
                            <label for="exampleInputUsername1">ItemName</label>
                            <input id="itemname" asp-for="ItemName"  type="text" class="form-control" placeholder="ItemName">
                        </div>

                        <div class="form-group">
                            <label for="exampleInputUsername1">SalesPrice</label>
                            <input id="salesprice" asp-for="SalesPrice" type="text" class="form-control" placeholder="SalesPrice">
                        </div>

                        <div class="form-group">
                            <label for="exampleInputUsername1">PurchasePrice</label>
                            <input id="purchase" asp-for="PurchasePrice" type="text" class="form-control" placeholder="PurchasePrice">
                        </div>

                        <div class="form-group">
                            <label for="exampleInputUsername1">Weight</label>
                            <input id="weight" asp-for="Weight" type="text" class="form-control" placeholder="Weight">
                        </div>

                    </div>
                </div>
            </div>

          


        </div>
    </form>
            <button onclick="login()" class="btn btn-gradient-primary me-2">Save</button>

</div>

@section Scripts
{
   
    <script src="~/modules/helper.js"></script>

    <script>
        function login(){
            var itemname=$("#itemname").val();
            var salesprice=$("#salesprice").val();
            var purchase=$("#purchase").val();
            var weight=$("#weight").val();
            
             $.ajax({
            url: "/admin/ajax/save",
            data: {
            itemname:itemname,
            salesprice:salesprice,
            purchaseprice:purchase,
            weight:weight
            },
            contenttype: "application/json; charset=utf-8",
            datatype: "json",
            type: "POST",
            success: function (data) {
                success(data);
                return data;
            },
            error: function (xhr, err) {
            
                $("#info").html("<p>an error has occurred</p>");
                return 0;
            }
            
        });
        // helper.ajaxcallpost("/admin/ajax/save",{itemname:itemname,
        //  salesprice:salesprice,
        //  purchaseprice:purchase,
        //  weight:weight},  function (data) {  }
                
        //     , function () { });
        }
    </script>
}


